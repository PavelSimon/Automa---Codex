feat(auth/ui): registration, gated content, profile edit and password change

- Domain: add User.full_name
- API: /api/v1/auth/register; /api/v1/users/me (PATCH), /api/v1/users/me/change_password
- UI: register form, content shown only when logged in; profile edit + password change via HTMX
- Templates and styles updated
